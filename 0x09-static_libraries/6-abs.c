#include "main.h"

/**
 * _abs - Check main
 * r: An integer
 * Description: function that computes the absolute value of an integer
 * Return: Absolute value of r
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
